---
- name: Debug task
  debug:
    msg: "START ansible.yang.configure junos/basic.yaml on connection={{ ansible_connection }}"

- name: Debug task
  debug:
    msg: "END ansible.yang.configure junos/basic.yaml on connection={{ ansible_connection }}"
