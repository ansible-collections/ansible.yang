---
- debug: msg="START ansible.yang.configure junos/basic.yaml on connection={{ ansible_connection
    }}"

- debug: msg="END ansible.yang.configure junos/basic.yaml on connection={{ ansible_connection
    }}"
